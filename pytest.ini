[pytest]
testpaths = tests
addopts = -ra --alluredir=allure-results --html=report.html --self-contained-html
markers =
    crud: happy-path create/read/update/delete journeys
    auth: authentication and write-authorization behaviour
    negative: invalid input / bad-payload / unhappy-path handling
    boundary: edge values such as zero price or date boundaries
